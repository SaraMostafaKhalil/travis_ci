language: node_js
node_js:
  - 13

services:
  - docker

# Pre-testing installs
install:
  - echo "nothing needs to be installed"

# Scripts to be run such as tests
before_script:
  - echo "no tests"

script:
  - docker --version 1 # print the version for logging
  - docker build -t simple-node .
  - docker tag simple-node saramostafa331/simple-node:latest

after_success:
  - docker login -u saramostafa331 -P Ud3uRcKQ5sqXie@
  - docker push saramostafa331/simple-node